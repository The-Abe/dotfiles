#!/bin/bash

if [ ! -x /usr/bin/kpcli ]
then
  echo "Install dependencies"
  sudo apt-get install kpcli libterm-readline-gnu-perl libdata-password-perl
fi

read -p "Find: " f
kpcli --kdb=/home/abe/abe.kdbx --command "find $f"

read -p "Press enter to continue"
