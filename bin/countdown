#!/bin/bash
minutes=$1
let seconds=$minutes*60

start="$(($(date +%s) + $seconds))"

echo $start > /tmp/$(whoami)_countdown_start
while [ "$start" -ge `date +%s` ]; do
    time="$(( $start - `date +%s` ))"
    printf '%s\r' "$(date -u -d "@$time" +%H:%M:%S)"
done
