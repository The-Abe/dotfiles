# vim:ft=swayconfig

# ── Program launchers ─────────────────────────────────────────────────────────
bindsym $mod+Return exec ~/.config/rofi-whichkey/rofi-whichkey.py
bindsym $mod+d      exec rofi -show combi
bindsym $mod+s      exec ~/bin/kp
bindsym $mod+x      exec kitty -e sh -c 'swaymsg -t get_tree | jq; echo; echo "press any key to close"; read -n1'
bindsym Ctrl+$mod+v exec ~/.config/sway/scripts/clipmenu.sh

# ── Media keys ────────────────────────────────────────────────────────────────
bindsym XF86AudioRaiseVolume exec "pactl set-sink-volume @DEFAULT_SINK@ +5%; ~/.local/bin/showvolume"
bindsym XF86AudioLowerVolume exec "pactl set-sink-volume @DEFAULT_SINK@ -5%; ~/.local/bin/showvolume"
bindsym XF86AudioMute        exec "pactl set-sink-mute @DEFAULT_SINK@ toggle; ~/.local/bin/showvolume"
bindsym XF86AudioPlay        exec playerctl play-pause
bindsym XF86AudioPause       exec playerctl play-pause
bindsym XF86AudioNext        exec playerctl next
bindsym XF86AudioPrev        exec playerctl previous
bindsym button4              exec playerctl next
bindsym button5              exec playerctl previous

# ── Brightness keys ───────────────────────────────────────────────────────────
bindsym XF86MonBrightnessUp   exec "brightnessctl set 100+; ~/.local/bin/showbrightness"
bindsym XF86MonBrightnessDown exec "brightnessctl set 100-; ~/.local/bin/showbrightness"

# ── Screenshots (grim/slurp replace flameshot) ────────────────────────────────
bindsym Print       exec ~/.local/bin/screenshot full
bindsym Shift+Print exec ~/.local/bin/screenshot area

# ── Workspace switching ($mod+<key>) and moving ($mod+Shift+<key>) ────────────
bindsym $mod+q       workspace $ws1
bindsym $mod+w       workspace $ws2
bindsym $mod+e       workspace $ws3
bindsym $mod+r       workspace $ws4
bindsym $mod+t       workspace $ws5
bindsym $mod+y       workspace $ws6
bindsym $mod+u       workspace $ws7
bindsym $mod+i       workspace $ws8
bindsym $mod+o       workspace $ws9
bindsym $mod+p       workspace $ws10
bindsym $mod+Shift+q move container to workspace $ws1
bindsym $mod+Shift+w move container to workspace $ws2
bindsym $mod+Shift+e move container to workspace $ws3
bindsym $mod+Shift+r move container to workspace $ws4
bindsym $mod+Shift+t move container to workspace $ws5
bindsym $mod+Shift+y move container to workspace $ws6
bindsym $mod+Shift+u move container to workspace $ws7
bindsym $mod+Shift+i move container to workspace $ws8
bindsym $mod+Shift+o move container to workspace $ws9
bindsym $mod+Shift+p move container to workspace $ws10

# ── Window focus and movement (vim-style hjkl) ────────────────────────────────
bindsym $mod+h       focus left
bindsym $mod+j       focus down
bindsym $mod+k       focus up
bindsym $mod+l       focus right
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Move the current workspace to another monitor
bindsym $mod+Ctrl+h move workspace to output left
bindsym $mod+Ctrl+j move workspace to output down
bindsym $mod+Ctrl+k move workspace to output up
bindsym $mod+Ctrl+l move workspace to output right

# ── Layout ────────────────────────────────────────────────────────────────────
bindsym $mod+b        split horizontal
bindsym $mod+v        split vertical
bindsym $mod+f        fullscreen toggle
bindsym $mod+n        layout toggle all
bindsym $mod+space    floating toggle
bindsym $mod+Ctrl+r   mode "resize"

# ── Sway management ───────────────────────────────────────────────────────────
bindsym $mod+Shift+c reload
bindsym $mod+Shift+Escape kill
bindsym $mod+Escape  exec swaylock

# ── Notifications ─────────────────────────────────────────────────────────────
bindsym Ctrl+Escape exec makoctl dismiss -a

# ── Resize mode ───────────────────────────────────────────────────────────────
mode "resize" {
  bindsym h       resize grow   left  5 px
  bindsym Shift+h resize shrink left  5 px
  bindsym j       resize grow   down  5 px
  bindsym Shift+j resize shrink down  5 px
  bindsym k       resize grow   up    5 px
  bindsym Shift+k resize shrink up    5 px
  bindsym l       resize grow   right 5 px
  bindsym Shift+l resize shrink right 5 px
  bindsym Return  mode "default"
  bindsym Escape  mode "default"
}

# ── Misc ──────────────────────────────────────────────────────────────────────
bindsym $mod+a   exec ~/.config/nvim/add_note.sh
bindsym $mod+Tab exec kitty --class "float" -e /usr/bin/zsh -ic "nv-daily"
bindsym $mod+Ctrl+p exec ~/.config/i3/pomodoro-toggle.sh toggle
